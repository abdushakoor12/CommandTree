{
  "name": "test/project",
  "scripts": {
    "test": "phpunit",
    "lint": "phpcs",
    "build": "echo Building...",
    "deploy": ["composer build", "rsync -avz dist/ server:/app/"],
    "pre-install-cmd": "echo pre-install",
    "post-install-cmd": "echo post-install"
  },
  "scripts-descriptions": {
    "test": "Run PHPUnit tests",
    "lint": "Run PHP CodeSniffer"
  }
}
